// RaceDetail: the page shown when you click a race (placeholder for now)
// We'll fill it with real race data in the next steps
function RaceDetail () {
    return (
        <div className="app">
            <h1 className="title">Race Detail</h1>
            <p>This is where a single race's details will go</p>
        </div>
    )
}

export default RaceDetail